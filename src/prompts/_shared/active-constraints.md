## Active Constraints (from project config)

The following constraints come from the project's DRYKISS configuration. They are *in addition* to the lens's default rules, not replacements. Honor them in your review.

{{active_constraints}}

If the section above is empty (placeholder not substituted), there are no project-level constraints active for this run.
